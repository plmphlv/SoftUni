function matrix(line){
for (let index = 0; index < line; index++) {
    const element = [];
    for (let index = 0; index < line; index++) {
        element.push(line)
    }
    console.log(element.join(" "));
}
}

matrix(7)